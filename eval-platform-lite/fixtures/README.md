# Fixtures

这里用于放公开展示版的脱敏样例数据。

当前仓库的主要可点击演示在 `../eval-center/index.html`，该页面内置静态 fixture，不依赖后端数据库。`eval-platform-lite` 保留真实前后端代码结构，便于面试官查看实现方式。

如果需要让 lite 后端跑出一组本地数据，可以在这里新增 seed JSON 或 seed 脚本，但不要提交真实平台账号、Cookie、原始抓取 HTML、真实运行数据库或未脱敏 trace。
